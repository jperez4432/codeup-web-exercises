// while loop
//Pizza Loop

//var slices = 8;
//while (slices > 0 ) {
//   console.log("ill have a slice!");
//    slices = slices - 1;
//    console.log("now theres " + slices + " slices left");
//}


// Pseudocode

// ask the user for input
//get the user input
// declare a user input variable
//store the user input in the variable
//declare a variable to store the total (accumulator )
// add the cost of the new item to the total
//keep storing the new totals in the accumulator
//tell the user how to end the program (sentinel value)
// loop back around and di it again until the user tells us to stop
/*
var priceOfItem = parseFloat(prompt("Enter the price of your first item"));
alert("The price of your item was " + priceOfItem);
var totalCost = priceOfItem;
var userInput = "";
while(userInput !=="STOP") {
    userInput = prompt("Enter the price of ypur next item \r\n Enter STOP when you are done")
    if (userInput === "STOP" {
        alert("OK, Your final total is " + totalCost);
} else {
    priceOfItem = parseFloat(prompt(":Enter the price of your next item \r\n Enter STOP when you are done"));
    totalCost = totalCost + priceOfItem;
    alert("Your total is now " + totalCost.toFixed(2));
        }
}
*/
/*
 var counter = 2;
 while (counter <= 65536) {
     console.log(counter);
     counter = counter * 2;
 }

var multiplyBy2 = 2;
while (multiplyBy2 <= 65536) {
    console.log(multiplyBy2);
    multiplyBy2 = multiplyBy2 * 2;
}

var count = 100;
while (count <= 100){
    console.log(count);
    count = count -1;
}))
*/

//for (count = 0; count <= 100; count = count+1){
//    console.log(count);
//}
//for (i = 0; 9<=100; i++){
//    console.log(i);
//}
